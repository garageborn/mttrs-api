h2 Total: #{ number_with_delimiter(model.total_count) }

= concept('admin/publisher/cell/uncategorized_links', nil, id: :uncategorized_links,
  name: :publisher_id, value: :slug, selected: params[:publisher_slug])

table.links
  thead
    tr
      th Url
      th Total Social
      th Publisher
      th Published At
  tbody
    = concept('admin/link/cell/uncategorized_item', collection: model)

= paginate(model)
