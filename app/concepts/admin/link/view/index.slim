h2
  = link_to 'New Link', [:new, :admin, :link], class: 'links-add-button'
  = link_to 'Uncategorized links', [:uncategorized, :admin, :links], class: 'links-add-button'

table.links
  thead
    tr
      th Title
      th Total Social
      th Publisher
      th Categories
      th Published At
      th Story ID
      th
  tbody
    = concept('admin/link/cell/item', collection: model)

= paginate(model)
