tr
  td= publisher_name
  td= order
  td= category_name
  td
    - if url_matcher.present?
      i url:
      span.category-matchers-matcher  #{ url_matcher }

    - if html_matcher.present?
      i html matcher:
      span.category-matchers-matcher  #{ html_matcher }

      i html selector:
      span.category-matchers-matcher #{ html_matcher_selector }

  td= link_to 'Edit', [:edit, :admin, model], class: 'category-matchers-edit-button'
