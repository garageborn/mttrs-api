machine:
  services:
    - redis
  ruby:
    version: 2.4.0

deployment:
  hub:
    branch: master
    commands:
      - bundle exec cap production deploy branch=master

test:
  override:
    - RAILS_ENV=test bundle exec rspec
