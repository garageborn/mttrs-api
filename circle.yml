machine:
  services:
    - docker
    - redis
  ruby:
    version: 2.3.3

dependencies:
  cache_directories:
    - ~/docker

deployment:
  hub:
    branch: master
    commands:
      - bin/rails deploy

test:
  override:
    - RAILS_ENV=test bundle exec rspec
