machine:
  services:
    - docker
  post:
    - eval `aws ecr get-login`

dependencies:
  cache_directories:
    - ~/docker

  override:
    - docker --version
    - docker info
    - ./.docker/build

deployment:
  hub:
    branch: docker
    commands:
      - ./.docker/push
      - ./.docker/publish

database:
  override:
    - echo 'no database setup'

test:
  override:
    - echo 'Skip'
