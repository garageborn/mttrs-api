machine:
  services:
    - docker
  post:
    - eval `aws ecr get-login`

dependencies:
  cache_directories:
    - ~/docker

  override:
    - cd ./.docker && ./build

deployment:
  hub:
    branch: master
    commands:
      - cd ./.docker && ./push
      - cd ./.docker && ./publish
